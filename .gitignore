# C# / .NET
[Bb]in/
[Oo]bj/
Release/
*.user
*.suo
*.userosscache
*.sln.docstates
*.dll
*.exe

# Except the ones we want to keep if we had binary releases in repo, but usually we don't.
# We will explicitly allow the Release folder if the user wants it, but standard practice is ignoring it.
# Wait, user said "gereksizleri aktarma", so we ignore compiled output.
# If they want the Release binaries on GitHub, they should use GitHub Releases.
App_Yayin/

# AI Agent Configs
.agents/
out/
